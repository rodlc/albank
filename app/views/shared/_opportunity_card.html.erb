<div class="card mb-3">
  <div class="card-body">
    <div class="row align-items-center">
      <div class="col-md-4">
        <h5 class="card-title mb-1">💡 Opportunité détectée</h5>

        <p class="text-muted mb-0">
          <small>
            Moyenne du marché:
            <%= number_to_currency(opportunity.standard.average_amount, unit: "€") %>
          </small>
        </p>
      </div>

      <div class="col-md-4 text-center">
        <div class="d-flex flex-column">
          <span class="text-muted small">Économies potentielles</span>

          <span class="h3 text-success mb-0">
            <%= number_to_currency(opportunity.savings, unit: "€") %>
          </span>
        </div>
      </div>

      <div class="col-md-4 text-end">
        <span
          class="
            badge bg-<%= case opportunity.status
            when 'pending' then 'warning'
            when 'contacted' then 'info'
            when 'completed' then 'success'
            end %> mb-2
          "
        >
          <%= opportunity.status.humanize %>
        </span>

        <br>
        <%= link_to "Voir les détails →", opportunity, class: "btn btn-primary btn-sm" %>
      </div>
    </div>
  </div>
</div>
