<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-soft d-print-none">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center" do %>
      <span class="fs-4 me-2" aria-hidden="true">🕵️</span>
      <span class="text-primary fs-5 letter-spacing-tight">
        AlBank
      </span>
    <% end %>

    <button
      class="navbar-toggler border-0 shadow-none"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Ouvrir le menu de navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center gap-3 gap-lg-2 mt-3 mt-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item w-100 w-lg-auto">
            <%= link_to statements_path,
                class: "btn #{current_page?(statements_path) ? 'btn-gov-primary' : 'btn-gov-outline'} w-100 w-lg-auto" do %>
              <span class="me-1" aria-hidden="true">📊</span>
              Mes relevés
            <% end %>
          </li>

          <li class="nav-item dropdown w-100 w-lg-auto">
            <button
              class="btn btn-ghost d-flex align-items-center justify-content-center w-100 w-lg-auto gap-2"
              id="navbarDropdown"
              type="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
              aria-label="Menu utilisateur"
            >
              <div
                class="d-flex align-items-center justify-content-center bg-primary text-white rounded-circle"
                style="width: 32px; height: 32px; font-weight: 600; font-size: 0.875rem;"
                aria-hidden="true"
              >
                <%= current_user.email.first.upcase %>
              </div>
              <span class="d-lg-none text-semibold">Mon compte</span>
            </button>

            <ul
              class="dropdown-menu dropdown-menu-end border-0 shadow-card mt-2"
              aria-labelledby="navbarDropdown"
            >
              <li>
                <span class="dropdown-item-text text-muted small">
                  <%= truncate(current_user.email, length: 30) %>
                </span>
              </li>

              <li><hr class="dropdown-divider my-2"></li>

              <li>
                <%= link_to destroy_user_session_path,
                    data: { turbo_method: :delete },
                    class: "dropdown-item text-danger d-flex align-items-center gap-2" do %>
                  <span aria-hidden="true">🚪</span>
                  Se déconnecter
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item w-100 w-lg-auto">
            <%= link_to new_user_session_path,
                class: "btn btn-gov-outline w-100 w-lg-auto" do %>
              Se connecter
            <% end %>
          </li>

          <li class="nav-item w-100 w-lg-auto">
            <%= link_to new_user_registration_path,
                class: "btn btn-gov-primary w-100 w-lg-auto" do %>
              S'inscrire
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
