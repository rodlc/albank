<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center" do %>
      <i class="fas fa-piggy-bank me-2" style="color: #9FE870; font-size: 1.5rem;"></i>
      <span style="color: #163300;">AlBank</span>
    <% end %>

    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to statements_path,
                class: "nav-link px-3 #{'fw-semibold' if current_page?(statements_path)}" do %>
              <i class="fas fa-file-invoice me-1"></i>
              Mes relevés
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to destroy_user_session_path,
                data: { turbo_method: :delete },
                class: "nav-link px-3 text-muted" do %>
              <i class="fas fa-sign-out-alt me-1"></i>
              Se déconnecter
            <% end %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to new_user_session_path,
                class: "nav-link px-3" do %>
              Se connecter
            <% end %>
          </li>
          <li class="nav-item">
            <%= link_to new_user_registration_path,
                class: "btn btn-success btn-sm ms-2 px-3" do %>
              S'inscrire
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
