<% if opportunity&.completed? %>
  <%= link_to statement_expense_opportunity_path(statement, expense, opportunity),
              class: "btn btn-soft w-100" do %>
    👀 Voir
  <% end %>
<% elsif opportunity&.contacted? %>
  <%= link_to statement_expense_opportunity_path(statement, expense, opportunity),
              class: "btn btn-success w-100" do %>
    ✅ Clôturer
  <% end %>
<% elsif opportunity&.danger? %>
  <%= link_to statement_expense_opportunity_path(statement, expense, opportunity),
              class: "btn btn-danger w-100" do %>
    🚨 Alerter
  <% end %>
<% elsif opportunity %>
  <%= link_to statement_expense_opportunity_path(statement, expense, opportunity),
              class: "btn btn-gov-primary w-100" do %>
    <i class="fas fa-envelope me-2"></i> Contacter
  <% end %>
<% end %>
