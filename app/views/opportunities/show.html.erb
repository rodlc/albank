<div class="container mt-5" data-controller="clipboard">
  <div class="mb-4 d-print-none">
    <%= link_to @statement, class: "text-decoration-none text-muted small fw-bold" do %>
      <span class="me-1">←</span> RETOUR AU RELEVÉ
    <% end %>
  </div>

  <div class="row justify-content-center">
    <div class="col-lg-10">
      <!-- Stepper -->
      <div class="d-print-none mb-4">
        <div class="position-relative mx-auto" style="max-width: 400px;">
          <div
            class="position-absolute start-0 w-100 bg-secondary bg-opacity-25"
            style="height: 2px; top: 16px; z-index: 0;"
          ></div>

          <div
            class="position-absolute start-0 bg-primary transition-width"
            style="height: 2px; top: 16px; z-index: 0; width: <%= @opportunity.completed? ? '100%' : (@opportunity.contacted? ? '50%' : '0%') %>;"
          ></div>

          <div
            class="d-flex justify-content-between position-relative"
            style="z-index: 1;"
          >
            <!-- Step 1 -->
            <div class="text-center">
              <div
                class="
                  rounded-circle bg-primary text-white d-flex
                  align-items-center justify-content-center mx-auto mb-2
                  shadow-sm
                "
                style="width: 32px; height: 32px; font-size: 1.2rem;"
              >
                🕵️
              </div>

              <span class="small fw-bold text-primary">Détectée</span>
            </div>

            <!-- Step 2 -->
            <div class="text-center">
              <div
                class="rounded-circle <%= @opportunity.contacted? || @opportunity.completed? ? 'bg-primary text-white' : 'bg-white border text-muted' %> d-flex align-items-center justify-content-center mx-auto mb-2 shadow-sm"
                style="width: 32px; height: 32px; font-size: 1.2rem;"
              >
                ✉️
              </div>

              <span class="small fw-bold <%= @opportunity.contacted? || @opportunity.completed? ? 'text-primary' : 'text-muted' %>">
                Envoyée
              </span>
            </div>

            <!-- Step 3 -->
            <div class="text-center">
              <div
                class="rounded-circle <%= @opportunity.completed? ? 'bg-primary text-white' : 'bg-white border text-muted' %> d-flex align-items-center justify-content-center mx-auto mb-2 shadow-sm"
                style="width: 32px; height: 32px; font-size: 1.2rem;"
              >
                🎉
              </div>

              <span class="small fw-bold <%= @opportunity.completed? ? 'text-primary' : 'text-muted' %>">
                Validée
              </span>
            </div>
          </div>
        </div>
      </div>

      <!-- Intro lettre -->
      <div class="text-center mb-4 d-print-none">
        <h3 class="h5 fw-bold">Modèle de lettre prêt à l'emploi</h3>

        <p class="text-muted small mb-4">
          Utilisez ce modèle pour négocier avec votre fournisseur
        </p>

        <!-- CTAs -->
        <div class="d-flex flex-wrap justify-content-center align-items-center gap-3">
          <button
            type="button"
            class="btn btn-primary fw-bold rounded-pill px-5 py-3 shadow-sm"
            data-clipboard-target="button"
            data-action="click->clipboard#copy"
          >
            <span class="me-2">📋</span> Copier
          </button>

          <button
            type="button"
            class="btn btn-outline-secondary rounded-pill px-4 py-2"
            onclick="window.print()"
          >
            <span class="me-2">🖨️</span> Imprimer
          </button>

          <%= form_with(model: [@statement, @expense, @opportunity], method: :patch, local: true, class: "d-inline m-0") do |f| %>
            <% unless @opportunity.contacted? || @opportunity.completed? %>
              <%= f.button name: "opportunity[status]", value: "contacted", class: "btn btn-outline-secondary rounded-pill px-4 py-2" do %>
                <span class="me-2">✉️</span> Envoyer
              <% end %>
            <% end %>

            <% if @opportunity.contacted? && !@opportunity.completed? %>
              <%= f.button name: "opportunity[status]", value: "completed", class: "btn btn-outline-secondary rounded-pill px-4 py-2" do %>
                <span class="me-2">🎉</span> Confirmer
              <% end %>
            <% end %>

            <% if @opportunity.completed? %>
              <span class="text-success fw-bold">
                <span class="me-1">✅</span> Clôturé
              </span>
            <% end %>
          <% end %>
        </div>

        <!-- Feedback copie -->
        <div
          data-clipboard-target="feedback"
          class="
            alert bg-success text-white text-center d-none border-0 fw-bold
            rounded-pill mt-3
          "
          role="alert"
        >
          <span class="me-2">✅</span> Copié!
        </div>
      </div>

      <%= render 'negotiation_letter' %>
    </div>
  </div>
</div>
