<div class="container mt-5">
  <div class="mb-4">
    <%= link_to @statement, class: "text-decoration-none text-muted small fw-bold" do %>
      <i class="fas fa-arrow-left me-1"></i>
      RETOUR AU RELEVÉ
    <% end %>
  </div>

  <div class="row justify-content-center">
    <div class="col-lg-10">
      <!-- Victory Header -->
      <div class="text-center mb-5">
        <div
          class="
            d-inline-flex align-items-center justify-content-center
            bg-success bg-opacity-10 text-success rounded-circle mb-3
          "
          style="width: 64px; height: 64px;"
        >
          <i class="fas fa-trophy fs-3"></i>
        </div>

        <h1 class="fw-bold mb-2">Opportunité détectée</h1>

        <p class="text-muted">
          Sur votre contrat
          <strong><%= @opportunity.expense.category.name %></strong>
        </p>
      </div>

      <!-- Main Card -->
      <div
        class="card border-0 shadow-lg overflow-hidden mb-5"
        style="border-radius: 16px;"
      >
        <div class="row g-0">
          <!-- Left: Current Situation -->
          <div class="col-md-5 bg-light border-end">
            <div class="p-4 p-md-5 h-100 d-flex flex-column justify-content-center">
              <span class="text-uppercase text-muted fw-bold small mb-2">
                Votre situation actuelle
              </span>

              <div class="d-flex align-items-baseline">
                <span class="display-5 fw-bold text-muted">
                  <%= number_to_currency(@opportunity.expense.subtotal, unit: "€", precision: 0) %>
                </span>

                <span class="text-muted ms-2">/ mois</span>
              </div>

              <p class="text-muted small mt-2 mb-0">
                Basé sur votre prélèvement du
                <%= l(@opportunity.expense.statement.date, format: "%B %Y") %>
              </p>
            </div>
          </div>

          <!-- Right: The Win -->
          <div class="col-md-7 bg-white">
            <div
              class="
                p-4 p-md-5 h-100 d-flex flex-column justify-content-center
                position-relative overflow-hidden
              "
            >
              <!-- Background decoration -->
              <div class="position-absolute top-0 end-0 p-3 opacity-10">
                <i class="fas fa-piggy-bank fa-10x text-success"></i>
              </div>

              <span
                class="
                  badge bg-success bg-opacity-10 text-success
                  align-self-start mb-3 px-3 py-2 rounded-pill
                "
              >
                <i class="fas fa-check-circle me-1"></i>
                Recommandé
              </span>

              <span class="text-uppercase text-success fw-bold small mb-2">
                Économie potentielle
              </span>

              <div class="d-flex align-items-baseline position-relative">
                <span class="display-3 fw-bold text-success">
                  <%= number_to_currency(@opportunity.savings, unit: "€", precision: 0) %>
                </span>

                <span class="text-success fs-4 ms-2 fw-bold">/ mois</span>
              </div>

              <p class="text-muted mt-3 mb-0 position-relative">
                En passant à une offre moyenne du marché
                (<%= number_to_currency(@opportunity.standard.average_amount, unit: "€", precision: 0) %>)
              </p>
            </div>
          </div>
        </div>

        <!-- Action Footer -->
        <div class="bg-light border-top p-4">
          <div
            class="
              d-flex flex-column flex-md-row justify-content-between
              align-items-center gap-3
            "
          >
            <div class="d-flex align-items-center">
              <span class="opportunity-badge badge-<%= @opportunity.status %> me-3">
                <% status_emoji = case @opportunity.status
                     when 'pending' then '⚫'
                     when 'contacted' then '🔵'
                     when 'completed' then '🟢'
                     else '⚫'
                   end %>

                <%= status_emoji %>
                <%= @opportunity.status.humanize %>
              </span>

              <span class="text-muted small">
                <i class="fas fa-info-circle me-1"></i>
                Statut du dossier
              </span>
            </div>

            <%= form_with(model: [@statement, @expense, @opportunity], method: :patch, local: true, html: { class: "d-flex gap-2" }) do |f| %>
              <% unless @opportunity.contacted? || @opportunity.completed? %>
                <%= f.button name: "opportunity[status]", value: "contacted", class: "btn btn-primary fw-bold px-4 rounded-pill" do %>
                  <i class="fas fa-paper-plane me-2"></i>
                  J'ai envoyé la lettre
                <% end %>
              <% end %>

              <% if @opportunity.contacted? && !@opportunity.completed? %>
                <%= f.button name: "opportunity[status]", value: "completed", class: "btn btn-success fw-bold px-4 rounded-pill" do %>
                  <i class="fas fa-check me-2"></i>
                  Économie validée!
                <% end %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Negotiation Letter Section -->
      <div class="text-center mb-4">
        <i class="fas fa-arrow-down text-muted mb-2"></i>
        <h3 class="h5 fw-bold">Modèle de lettre prêt à l'emploi</h3>

        <p class="text-muted small">
          Utilisez ce modèle pour négocier avec votre fournisseur
        </p>
      </div>

      <%= render 'negotiation_letter' %>
    </div>
  </div>
</div>
