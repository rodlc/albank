<div class="container mt-5">
  <div class="mb-4">
    <%= link_to statements_path, class: "text-decoration-none text-muted small fw-bold" do %>
      <span class="me-1">←</span> RETOUR AUX RELEVÉS
    <% end %>
  </div>

  <div class="d-flex justify-content-between align-items-end mb-5">
    <div>
      <h1 class="fw-bold mb-1">
        <span class="underline-cyan">
          Relevé de <%= l(@statement.date, format: "%B %Y").capitalize %>
        </span>
      </h1>

      <p class="text-muted mb-0">Analyse détaillée des transactions</p>
    </div>

    <div class="text-end">
      <span class="d-block text-muted small text-uppercase fw-bold">
        Total Dépenses
      </span>

      <span class="fs-2 fw-bold">
        <%= number_to_currency(@expenses.sum(&:subtotal), unit: "€", precision: 0) %>
      </span>
    </div>
  </div>

  <!-- Summary Cards -->
  <% total_savings = @expenses.joins(:opportunities).sum { |e| e.opportunities.sum(&:savings) } %>

  <% if total_savings > 0 %>
    <div
      class="
        alert bg-coral-subtle d-flex align-items-center border-0 shadow-sm
        mb-5
      "
      role="alert"
    >
      <div class="fs-1 me-3">🕵️</div>

      <div>
        <h4 class="alert-heading fw-bold mb-1 text-coral">
          Potentiel d'économie détecté !
        </h4>

        <p class="mb-0">
          En optimisant vos contrats, vous pourriez économiser
          <strong class="fs-5 text-coral"><%= number_to_currency(total_savings, unit: "€", precision: 0) %> / mois</strong>.
        </p>
      </div>
    </div>
  <% end %>

  <% if @expenses.any? %>
    <div class="accordion expenses-accordion" id="expensesAccordion">
      <% grouped = @expenses.group_by { |e| e.opportunities.first&.result_type } %>

      <% [:danger, :opportunity, :success].each_with_index do |type, i| %>
        <% next unless (expenses = grouped[type.to_s])&.any? %>
        <%= render "expenses/accordion_section",
                   result_type: type,
                   expenses: expenses,
                   statement: @statement,
                   index: i %>
      <% end %>

      <% if (autres = grouped[nil])&.any? %>
        <%= render "expenses/accordion_section",
                   result_type: nil,
                   expenses: autres,
                   statement: @statement,
                   index: 3 %>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-5">
      <div class="mb-3 fs-1 opacity-50">
        🧾
      </div>

      <p class="text-muted">Aucune dépense trouvée dans ce relevé.</p>
    </div>
  <% end %>
</div>
